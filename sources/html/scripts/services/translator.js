/*Core.ServiceTranslator = Core.extend
(
	messages: null,
	translate: function(message, context)
	{
		if (!message || (message && (typeof message === 'string') && (message.length === 0)))
		{
			console.error('[ERROR] [Core.ServiceTranslator] @message cannot be null, empty or undefined');
		}
		else
		{
			if (context && (message.lastIndexOf('{{') !== -1))
			{}
		}
		
	}
);*/